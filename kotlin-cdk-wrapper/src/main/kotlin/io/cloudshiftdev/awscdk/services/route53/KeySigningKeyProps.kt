@file:Suppress("RedundantVisibilityModifier","RedundantUnitReturnType","RemoveRedundantQualifierName","unused","UnusedImport","ClassName","REDUNDANT_PROJECTION","DEPRECATION")

package io.cloudshiftdev.awscdk.services.route53

import io.cloudshiftdev.awscdk.common.CdkDslMarker
import io.cloudshiftdev.awscdk.common.CdkObject
import io.cloudshiftdev.awscdk.common.CdkObjectWrappers
import io.cloudshiftdev.awscdk.services.kms.IKey
import kotlin.String
import kotlin.Unit

/**
 * Properties for constructing a Key Signing Key.
 *
 * Example:
 *
 * ```
 * HostedZone hostedZone;
 * Key kmsKey;
 * KeySigningKey.Builder.create(this, "KeySigningKey")
 * .hostedZone(hostedZone)
 * .kmsKey(kmsKey)
 * .keySigningKeyName("ksk")
 * .status(KeySigningKeyStatus.ACTIVE)
 * .build();
 * ```
 */
public interface KeySigningKeyProps {
  /**
   * The hosted zone that this key will be used to sign.
   */
  public fun hostedZone(): IHostedZone

  /**
   * The name for the key signing key.
   *
   * This name must be unique within a hosted zone.
   *
   * Default: an autogenerated name
   */
  public fun keySigningKeyName(): String? = unwrap(this).getKeySigningKeyName()

  /**
   * The customer-managed KMS key that that will be used to sign the records.
   *
   * The KMS Key must be unique for each KSK within a hosted zone. Additionally, the
   * KMS key must be an asymetric customer-managed key using the ECC_NIST_P256 algorithm.
   *
   * [Documentation](https://docs.aws.amazon.com/Route53/latest/DeveloperGuide/dns-configuring-dnssec-cmk-requirements.html)
   */
  public fun kmsKey(): IKey

  /**
   * The status of the key signing key.
   *
   * Default: ACTIVE
   */
  public fun status(): KeySigningKeyStatus? =
      unwrap(this).getStatus()?.let(KeySigningKeyStatus::wrap)

  /**
   * A builder for [KeySigningKeyProps]
   */
  @CdkDslMarker
  public interface Builder {
    /**
     * @param hostedZone The hosted zone that this key will be used to sign. 
     */
    public fun hostedZone(hostedZone: IHostedZone)

    /**
     * @param keySigningKeyName The name for the key signing key.
     * This name must be unique within a hosted zone.
     */
    public fun keySigningKeyName(keySigningKeyName: String)

    /**
     * @param kmsKey The customer-managed KMS key that that will be used to sign the records. 
     * The KMS Key must be unique for each KSK within a hosted zone. Additionally, the
     * KMS key must be an asymetric customer-managed key using the ECC_NIST_P256 algorithm.
     */
    public fun kmsKey(kmsKey: IKey)

    /**
     * @param status The status of the key signing key.
     */
    public fun status(status: KeySigningKeyStatus)
  }

  private class BuilderImpl : Builder {
    private val cdkBuilder: software.amazon.awscdk.services.route53.KeySigningKeyProps.Builder =
        software.amazon.awscdk.services.route53.KeySigningKeyProps.builder()

    /**
     * @param hostedZone The hosted zone that this key will be used to sign. 
     */
    override fun hostedZone(hostedZone: IHostedZone) {
      cdkBuilder.hostedZone(hostedZone.let(IHostedZone::unwrap))
    }

    /**
     * @param keySigningKeyName The name for the key signing key.
     * This name must be unique within a hosted zone.
     */
    override fun keySigningKeyName(keySigningKeyName: String) {
      cdkBuilder.keySigningKeyName(keySigningKeyName)
    }

    /**
     * @param kmsKey The customer-managed KMS key that that will be used to sign the records. 
     * The KMS Key must be unique for each KSK within a hosted zone. Additionally, the
     * KMS key must be an asymetric customer-managed key using the ECC_NIST_P256 algorithm.
     */
    override fun kmsKey(kmsKey: IKey) {
      cdkBuilder.kmsKey(kmsKey.let(IKey::unwrap))
    }

    /**
     * @param status The status of the key signing key.
     */
    override fun status(status: KeySigningKeyStatus) {
      cdkBuilder.status(status.let(KeySigningKeyStatus::unwrap))
    }

    public fun build(): software.amazon.awscdk.services.route53.KeySigningKeyProps =
        cdkBuilder.build()
  }

  private class Wrapper(
    cdkObject: software.amazon.awscdk.services.route53.KeySigningKeyProps,
  ) : CdkObject(cdkObject), KeySigningKeyProps {
    /**
     * The hosted zone that this key will be used to sign.
     */
    override fun hostedZone(): IHostedZone = unwrap(this).getHostedZone().let(IHostedZone::wrap)

    /**
     * The name for the key signing key.
     *
     * This name must be unique within a hosted zone.
     *
     * Default: an autogenerated name
     */
    override fun keySigningKeyName(): String? = unwrap(this).getKeySigningKeyName()

    /**
     * The customer-managed KMS key that that will be used to sign the records.
     *
     * The KMS Key must be unique for each KSK within a hosted zone. Additionally, the
     * KMS key must be an asymetric customer-managed key using the ECC_NIST_P256 algorithm.
     *
     * [Documentation](https://docs.aws.amazon.com/Route53/latest/DeveloperGuide/dns-configuring-dnssec-cmk-requirements.html)
     */
    override fun kmsKey(): IKey = unwrap(this).getKmsKey().let(IKey::wrap)

    /**
     * The status of the key signing key.
     *
     * Default: ACTIVE
     */
    override fun status(): KeySigningKeyStatus? =
        unwrap(this).getStatus()?.let(KeySigningKeyStatus::wrap)
  }

  public companion object {
    public operator fun invoke(block: Builder.() -> Unit = {}): KeySigningKeyProps {
      val builderImpl = BuilderImpl()
      return Wrapper(builderImpl.apply(block).build())
    }

    internal fun wrap(cdkObject: software.amazon.awscdk.services.route53.KeySigningKeyProps):
        KeySigningKeyProps = CdkObjectWrappers.wrap(cdkObject) as? KeySigningKeyProps ?:
        Wrapper(cdkObject)

    internal fun unwrap(wrapped: KeySigningKeyProps):
        software.amazon.awscdk.services.route53.KeySigningKeyProps = (wrapped as
        CdkObject).cdkObject as software.amazon.awscdk.services.route53.KeySigningKeyProps
  }
}
